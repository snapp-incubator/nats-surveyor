---
apiVersion: v2
name: nats-surveyor
description: NATS Surveyor Helm chart for monitoring NATS servers and JetStream
type: application
# version and appVersion are overridden by the release workflow at package time
# from the git tag. These defaults exist so local `helm install` from a checkout
# produces something runnable.
version: 0.10.1
appVersion: "0.10.1"

